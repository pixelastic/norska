(async () => {
  // Take screenshots and open the percy build
  // Should onl take screensots of the ./tailwind dir
  // and only the .screenshot classes
  // Open the change, and wait for validation
}();
