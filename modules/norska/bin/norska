#!/usr/bin/env node
/* eslint-disable import/no-commonjs */
const norska = require('../build/index').default;
const minimist = require('minimist');
const args = minimist(process.argv.slice(2));
(async () => {
  await norska.run(args);
})();
